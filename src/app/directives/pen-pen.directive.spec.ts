import { PenPenDirective } from './pen-pen.directive';

describe('PenPenDirective', () => {
  it('should create an instance', () => {
    const directive = new PenPenDirective();
    expect(directive).toBeTruthy();
  });
});
